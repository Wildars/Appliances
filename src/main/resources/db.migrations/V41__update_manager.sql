CREATE TABLE shift_schedule
(
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    manager_id  BIGINT,
    shift_start TIMESTAMP WITHOUT TIME ZONE,
    shift_end   TIMESTAMP WITHOUT TIME ZONE,
    filial_id   BIGINT,
    CONSTRAINT pk_shiftschedule PRIMARY KEY (id)
);

ALTER TABLE shift_schedule
    ADD CONSTRAINT FK_SHIFTSCHEDULE_ON_FILIAL FOREIGN KEY (filial_id) REFERENCES filial (id);

ALTER TABLE shift_schedule
    ADD CONSTRAINT FK_SHIFTSCHEDULE_ON_MANAGER FOREIGN KEY (manager_id) REFERENCES manager (id);





ALTER TABLE manager ADD COLUMN filial_id BIGINT;

ALTER TABLE manager
    ADD CONSTRAINT FK_MANAGER_ON_FILIAL FOREIGN KEY (filial_id) REFERENCES filial (id);